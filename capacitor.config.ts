import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "instaclone",
  appName: "i4Checkpoint",
  bundledWebRuntime: false,
  webDir: "www",
};

export default config;
